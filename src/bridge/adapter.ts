/**
 * Backward-compatible type façade re-exporting the canonical BridgePort
 * adhering to the Interface Segregation Principle (ISP).
 */
export type { BridgePort as BridgeAdapter } from "../core/ports/bridge-port.js";
